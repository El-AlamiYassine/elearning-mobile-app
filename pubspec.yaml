name: elearning
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  dio: ^5.9.2  # HTTP client for Dart, used for making API requests.
  shared_preferences: ^2.5.5 # Used for storing simple data on the device, such as user preferences or authentication tokens.
  go_router: ^17.2.3 # A routing package for Flutter that simplifies navigation and deep linking.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
